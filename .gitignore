# keep repo clean: ignore every markdown file except the public-facing ones
*.md
!README.md
!SECURITY.md

# python build artifacts
__pycache__/
*.pyc

# python env (uv)
.venv/
uv.lock

# test / coverage artifacts
.coverage
coverage.xml
.pytest_cache/

# OpenTofu / Terraform (keep .tf and .terraform.lock.hcl; ignore state + provider cache)
infra/.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars

# job runtime artifacts
trivy_report.json
sbom.cdx.json
fim_baseline.db
iam_audit.md
